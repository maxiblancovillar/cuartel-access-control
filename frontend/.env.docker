# Variables usadas al construir la imagen del frontend (Vite las embebe en build time).
# En docker-compose.yml se pasan como build args (ver docker-compose.yml).
VITE_API_URL=/api/v1
VITE_APP_NAME="Sistema de Control de Acceso"
VITE_APP_VERSION=1.0.0
